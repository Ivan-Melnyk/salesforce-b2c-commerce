<script type="text/javascript">
	var last_oms_order = "importPackage( dw.oms );\n"
		+ "importPackage( dw.customer );\n"
		+ "importPackage( dw.order );\n"
		+ "importScript( 'app_omscheckout_shim:utils/OmsOrderWrapper.ds' );\n\n"
		+ "var Orders = OMS.getSalesOrders(pdict.CurrentCustomer);\n"
		+ "debug(OmsOrderWrapper(Orders[0]));\n\n"
	;
	
</script>